# 2026-02-25

## Gathr progress log (resume anchor)
- WhatsApp gateway briefly disconnected (499) then reconnected; user confirmed it works.
- User resumed Gathr work and asked for continuity.
- Implemented anti-abuse rule: ratings allowed only after event end time.
  - Frontend (`gathr/App.tsx`):
    - Added `hasEventEnded(exactTime)`.
    - Blocks submit before event end with explicit error.
    - Hides “Rate host” button until event end.
  - Backend SQL:
    - Updated `gathr/supabase_safety_enforcement_patch.sql` to require `e.exact_time::timestamptz <= now()` in rating policies.
    - Added incremental patch `gathr/supabase_ratings_after_event_patch.sql`.
- User hit DB error: invalid timestamp syntax for `"16:00"`.
- Implemented UX fix in app:
  - Replaced manual exact-time typing with date-then-time picker.
  - Stores `exact_time` as ISO timestamp (`toISOString()`).
  - Added dependency `@react-native-community/datetimepicker` and Expo plugin config.
- User confirmed picker now works.

## User preference
- Wants reliable next-day continuity without re-pasting prior context.
- Keep a compact running milestone log in workspace memory after major steps.
- Prefers explicit run commands and rapid iteration; push to GitHub whenever a change is confirmed working.

## Gathr major progress (later sessions)
- Added searchable activity input + suggestions.
- Added event description field and DB patch.
- Added profile expansion: full name, gender, age group, based-in, interests picker, about me.
- Added invite flow upgrades:
  - host/member invites,
  - invitee accept/decline,
  - host invites auto-approve on accept,
  - invite inbox and statuses.
- Added per-participant post-event rating flow with skip support.
- Added activity-quality ratings (separate from person ratings).
- Added participant trust visibility and group averages in event cards.
- Added map browse mode and near-me default (global view admin-only).
- Added many UX improvements:
  - collapsible sections,
  - collapse-all/open-main controls,
  - improved header visibility,
  - web date picker modal + denser day grid,
  - web map-safe fallbacks.
- Capacity model migrated to min/max/no-max:
  - Create event uses min + max + no max checkbox,
  - capacity text and full checks updated accordingly.
- Privacy/visibility logic improved:
  - participant names hidden pre-event,
  - pre-event shows only counts + aggregate trust/skill,
  - post-event names visible only to host/approved attendees.
- Reporting improved:
  - report reason + optional details/comment,
  - moderation dashboard for admin (user `1`) with watchlist/reviewed/clear statuses,
  - notifications include reported-user reminders in relevant events.
- Added event comments/chat thread MVP:
  - host + approved attendees can post,
  - comments shown per event.
- Added invite suggestions from profiles with offline location matching (based_in vs event city), online remains global.

## Important SQL patches created today
- `gathr/supabase_ratings_after_event_patch.sql`
- `gathr/supabase_event_description_patch.sql`
- `gathr/supabase_profiles_patch.sql`
- `gathr/supabase_profile_extended_patch.sql`
- `gathr/supabase_event_coords_patch.sql`
- `gathr/supabase_event_capacity_patch.sql`
- `gathr/supabase_invites_patch.sql`
- `gathr/supabase_rating_skips_patch.sql`
- `gathr/supabase_activity_ratings_patch.sql`
- `gathr/supabase_event_overflow_patch.sql`
- `gathr/supabase_moderation_status_patch.sql`
- `gathr/supabase_report_details_patch.sql`
- `gathr/supabase_event_min_max_patch.sql`
- `gathr/supabase_event_comments_patch.sql`
